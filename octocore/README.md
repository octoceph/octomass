# OctoCore — fork of Videomass (alpha) with Audio DSP

Этот репозиторий — форк проекта **Videomass**, расширённый для задач OctoCore:
мультикамерного захвата, локальной и облачной ИИ-аналитики, интеграции с IPFS/Helia/Filecoin,
поддержки сенсоров (MQTT/Modbus/Serial) и — дополнительно — полноценного аудио-движка (DSP + ML).

> ВНИМАНИЕ: это рабочий прототип («skeleton») — предназначен для локальной разработки и PoC.
> Лицензия: GPL-3.0-or-later (как у оригинального Videomass).

## Что включено в этот форк
- FastAPI сервер с эндпойнтами для приёма кадров, snapshot RTSP, загрузки и анализа аудио.
- Модули для работы с веб-камерами (OpenCV) и IP-камерами (ffmpeg snapshot).
- Audio engine: DSP (librosa), эвристики, hooks для TF/PyTorch/ONNX inference.
- Простая интеграция с IPFS (ipfshttpclient / Infura fallback) и Filecoin shim.
- Сенсоры: MQTT, Modbus, Serial (каркас).
- Dockerfile и docker-compose для локального запуска.

## Быстрый старт (локально)
1. Скопируйте `.env.example` в `.env` и заполните переменные (INFURA_PROJECT_ID и т.д.), если планируете использовать IPFS/Powergate.
2. Установите зависимости (рекомендуется poetry):
   ```bash
   poetry install
   ```
   Или pip:
   ```bash
   pip install -e .
   ```
3. Запустите сервер разработки:
   ```bash
   uvicorn octocore.server:app --reload --port 8000
   ```
4. Тестовые вызовы:
   * Snapshot RTSP:
     ```
     GET http://localhost:8000/api/v1/rtsp/snapshot?url=rtsp://user:pass@host/stream
     ```
   * Upload frame:
     ```
     POST /api/v1/frames (multipart: file, metadata)
     ```
   * Upload audio and analyze:
     ```
     POST /api/v1/audio (multipart: file, metadata)  # returns analysis (or queued)
     ```

## Audio engine (DSP + ML)
Папка `src/octocore/audio/` содержит модули:
- `dsp.py` — загрузка аудио, melspec, mfcc, spectrogram, детекция событий по энергии.
- `ml_models.py` — адаптеры для TF/Torch/ONNX моделирования.
- `ingest.py` — pipeline обработки аудио, сохранение результатов и опциональный upload в IPFS.

Примеры использования в `server.py` — endpoint `/api/v1/audio`.


# Полное и понятное описание функционала (для непрофессионалов / студентов)

Ниже — простой и полный обзор того, **что умеет** OctoCore и какие **новые возможности** появляются в результате синергии с функционалом «видеомасс-подобного» проекта. Всё на русском, без тяжёлой технической терминологии — чтобы любой студент или обычный человек мог быстро понять, что делает система и как ею пользоваться.

---

## Коротко: что такое OctoCore (одной фразой)

OctoCore — это приложение/система для **захвата видео и аудио**, **анализa** этих данных (в том числе с помощью ИИ), **хранения** их в децентрализованных сетях (IPFS / Filecoin) и удобной работы с результатами через визуальный дашборд.

---

# Что умеет система — простой список функций

### 1. Подключение камер

* **Веб-камеры** (тот, что в ноутбуке или USB-kamera) — подключается прямо в браузере или приложении через разрешение камеры.
* **IP-камеры** (уличные/сетевые камеры) — вводите URL (rtsp/http), логин/пароль; если браузер не может напрямую — есть примерный сервер-прокси (Node/ffmpeg) для получения снимка.
* **Сканирование сети** — кнопка «Scan» выполняет поиск камер в локальной сети.

### 2. Управление камерами в дашборде

* Карточки-видео для каждой камеры (квадрат с видео).
* У каждой карточки — компактные кнопки:

  * Снимок (📸)
  * Запись видео (●)
  * Запустить/остановить интервал съёмки (покадровая съёмка)
  * Выбор ИИ-модели / запуск анализа
  * Параметры (интервал съёмки, имя камеры)
  * Удалить камеру (останавливает все потоки и освобождает ресурсы)
* Кнопка «Перезапустить все камеры» — заново запрашивает доступ и ребутит потоки.

### 3. Автоматические снимки / интервал-режим

* Для каждой камеры можно задать интервал в секундах. Нажали «Снимок» — если интервал > 0, начинается циклическая съёмка и кадры попадают в галерею.
* Есть кнопка «Старт / Стоп покадровой съёмки».

### 4. Галереи и таймлайны

* Создаёте галереи (проекты) — каждая галерея это горизонтальная лента кадров.
* Для каждого кадра — чекбокс; можно массово выбрать кадры.
* Операции: Выбрать все / Снять выбор / Удалить выбранные / Отправить в IPFS / Создать таймлапс.
* Таймлапс генерируется как видео из выбранных кадров (настраивается FPS).

### 5. Таймлапсы

* Создание видео из набора кадров. Можно выбирать:

  * Частоту кадров (FPS)
  * Разрешение (при поддержке)
  * Эффекты перехода (простые fade/zoom)
* Таймлапс сохраняется и может быть отправлен в IPFS/архив.

### 6. ИИ-анализ (очень просто)

* Встроенные модели:

  * **Sobel** (контуры) — вычисляет границы/контуры на изображении.
  * **MobileNet** (классификация) — определяет, что на картинке (топ-предсказания).
* Можно выбрать модель в глобальной панели, затем применять её к любой камере или кадру.
* Опции при анализе:

  * Наложить результаты поверх потока (overlay) — например, контуры или bounding boxes.
  * Сохранить результаты (теги/метаданные) вместе с кадром.
* Результаты анализа прикрепляются к каждому кадру или к таймлапсу (т.е. при просмотре кадра вы видите предсказания и confidence).

* Так же можно загрузить своии ИИ модели (Upload File) или подключить их со сторониих ресурсов чере API (GitVerse, Github, HugginFace).

### 7. Контуры и оценка биомассы растений (демо + базовый алгоритм)

* Контуры растений — рассчитываются через фильтры/свертки (Sobel/threshold) и даёт площадь в пикселях.
* По площади и известному масштабу можно приблизительно оценить биомассу (единицы: относительные баллы / площади).
* Эти данные прикрепляются к кадрам и таймлапсам вместе с CID при сохранении.

### 8. Audio Engine (DSP + ML)

* Загрузка и запись аудио (wav/mp3).
* Автоматический анализ: мел-спектрограмма (Melspec), MFCC, детекция всплесков энергии (событий).
* Примеры применения: обнаружение жужжания пчёл, криков птиц, звуков китов — через эвристики или готовые ML-модели.
* Результат анализа (events, метрики) прикрепляется к аудио-файлу и может быть сохранён в IPFS.

### 9. IPFS / CID Manager

* Сохранение кадров / таймлапсов / аудио в IPFS → получаете CID.
* CID Manager (в приложении): список CID, заметки, метаданные, копировать CID, удалять/редактировать записи.
* Можно восстановить/скачать файл по CID через публичный gateway.

### 10. Логи и уведомления

* Логи расположены в левом меню — как консоль: все действия и ошибки видны в читаемом виде.
* Вместо браузерных алёртов — плавные всплывающие уведомления (toasts) в углу интерфейса.

### 11. Встроенная IDE (простая)

* Редактор кода (CodeMirror) с темой Cobalt.
* Режимы: JavaScript (выполняется в среде приложения) и Python (Pyodide загружается по запросу).
* Можно написать простой скрипт и применить его к данным или моделям (опция «Применять к моделям»).

### 12. UI/UX особенности

* Левое фиксированное меню (с возможностью скрытия) — быстрый доступ ко вкладкам.
* Легко перетаскиваемая верхняя часть (title bar) — позволяет двигать окно.
* Настройки прозрачности и blur окна (для десктоп-версии Tauri): слайдеры в панели управления.
* Canvas/WebGL панель — занимают всё доступное пространство, там идёт анимация «распределённого хранения» (узлы, линии).

### 13. Плагины и расширяемость

* Плагин-API: вы можете добавлять собственные анализаторы и хранилища:

  * `registerAnalyzer(name, {run, overlay})`
  * `registerStorage(name, {save, fetch})`
* Это даёт гибкость: подключайте новые модели или свои провайдеры хранения.

---

# Как это использовать — простые пошаговые сценарии (для начинающих)

### Быстрая проверка за 5 минут

1. Откройте приложение/страницу.
2. Нажмите «Добавить веб-камеру» — развернётся диалог браузера, разрешите доступ к камере.
3. Увидите карточку с течением видео — нажмите 📸 → сделаете снимок, он попадёт в галерею.
4. Зайдите в вкладку «Gallery», выберите кадр и нажмите «Отправить в IPFS» → получите CID.

### Сделать таймлапс из камеры

1. На карточке камеры задайте интервал (например 60 с).
2. Нажмите «Старт покадровой съёмки».
3. После набора кадров зайдите в галерею → выберите кадры → «Создать таймлапс» → укажите FPS (например 24) → создайте видео.
4. Отправьте таймлапс в IPFS, сохраните CID.

### Быстрая ИИ-проверка

1. В панели ИИ выберите MobileNet.
2. На карточке камеры выберите модель в выпадающем списке и нажмите «Анализ».
3. Результаты появятся в виде всплывающего окна и/или будут наложены поверх видео (overlay).
4. Сохраните результаты вместе с кадром — в будущем можно фильтровать кадры по тегам (например: «растение», «человек», «транспорт»).

### Анализ звука (пример с ульем)

1. Загрузите файл hive\_sound.wav через вкладку Audio.
2. Нажмите «Запустить анализ» — система вычислит мел-спектрограмму и выдаст список событий (времен и оценок).
3. Просмотрите JSON-отчёт и посмотрите, какие участки отмечены как критичные.

---

# Что даёт синергия «видеомасс-подобных» функций + OctoCore (простыми словами)

Объединение возможностей «видеомасс-подобной» системы (многоисточниковый захват, ffmpeg-прокси, галереи, таймлапсы) с расширениями OctoCore (IPFS, audio DSP, встроенная IDE, плагинная архитектура) даёт сразу несколько реальных преимуществ:

1. **Единая платформа** — вместо десятка отдельных утилит у вас всё в одном: захват → анализ → хранение → поиск.
2. **Экономия времени и денег** — предобработка и фильтрация на месте (edge) позволяют отправлять в облако только важное.
3. **Привязка аналитики к артефактам** — к каждому кадру и таймлапсу можно прикрепить результаты ИИ и аудио-метрики (всё хранится вместе, в том числе в IPFS).
4. **Надёжность и доказательственность** — сохранение важных артефактов в IPFS/Filecoin делает данные неизменяемыми и доступными для аудита.
5. **Гибкость для исследований** — студенты и исследователи могут писать свои скрипты в IDE и быстро тестировать идеи на реальных данных.
6. **Широкая применимость** — от агропрома до экологии и строительного контроля — те же функции покрывают разные задачи.
7. **Масштабируемость** — архитектура позволяет наращивать: больше камер, больше моделей, платный архив в Filecoin.

---

# Ограничения и что важно знать (честно и прямо)

* **RTSP в браузере** часто заблокирован CORS — для реального подключения IP-камер нужен сервер-прокси с ffmpeg или WebRTC-gateway.
* **Точность ИИ** зависит от модели и качества кадров/аудио. Базовые модели (MobileNet/Sobel) — демонстрационные; для серьёзных задач потребуются дообученные или кастомные модели.
* **Оценка биомассы** через контуры даёт приближённую метрику; нужна калибровка (масштаб, дистанция, объектив).
* **Хранение в IPFS/Filecoin**: public CID делают данные доступны в сети — думайте о приватности; используйте шифрование при необходимости.
* **Производительность**: обработка видео и аудио на слабом ноутбуке может быть медленной; для больших нагрузок нужен сервер с GPU.

---

# Идеи простых проектов/лабораторных для студентов

1. **Выявление пчелиного «стресса»**: записать звук улья, проанализировать события энергии и сравнить до/после вмешательства.
2. **Таймлапс роста растения**: сделать покадровую съёмку, создать таймлапс и оценить изменение площади листьев (контуры → относительная биомасса).
3. **Классификатор птиц**: собрать звук/фото птиц, обучить простую модель (или использовать HuggingFace) и интегрировать результат в галерею.
4. **Системы оповещения**: настроить детектор движения/звука и отправлять уведомления (toasts) при тревоге.

---

# Простая «часто задаваемая» инструкция (FAQ)

**Q: Как подключить IP-камеру, если браузер не подключается?**
A: Используйте пример server-proxy (ffmpeg) — он берёт RTSP и отдаёт JPG или MJPEG, который уже можно загрузить в браузер.

**Q: Как сохранить кадр в IPFS?**
A: Нажмите «Отправить в IPFS» в галерее → получите CID, который можно скопировать и вернуться к кадру позже.

**Q: Как удалить камеру полностью?**
A: Нажмите «Удалить» на карточке камеры — это остановит треки, MediaRecorder и интервалы, и удалит элемент из интерфейса.

**Q: Нужно ли сохранять ключи OpenAI/HF в приложении?**
A: Лучше не сохранять их в localStorage. Вводите вручную при каждой сессии или используйте серверный прокси с хранилищем секретов.

---

# Словарь простыми словами

* **CID** — адрес файла в IPFS (как уникальный хэш).
* **IPFS** — сеть для хранения файлов по содержимому (доступ через CID).
* **RTSP** — протокол сетевой камеры для видеопотоков.
* **Sobel** — простой алгоритм для поиска контуров (границ на картинке).
* **MobileNet** — лёгкая модель для распознавания объектов (например: «растение», «человек»).

---
